import createAdapterManager from 'factories/createAdapterManager';
import entityRegister from 'vidgets/entityRegister';

const adapterManager = createAdapterManager({ entityRegister });

export default adapterManager;
